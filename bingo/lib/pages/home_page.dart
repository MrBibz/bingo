import 'package:flutter/material.dart';
import 'package:bingo/widgets/home_widget.dart';

class HomePage extends StatelessWidget {
  const HomePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Home(),
    );
  }
}
